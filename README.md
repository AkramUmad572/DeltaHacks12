# 1. Install backend
cd backend
npm install

# 2. Install frontend  
cd ../frontend
npm install

# 3. Run backend (Terminal 1)
cd ../backend
npm start

# 4. Run frontend (Terminal 2)
cd ../frontend
npm run dev

# 5. Open http://localhost:3000